version: 2
jobs:
  build:
    docker:
      - image: microsoft/aspnetcore-build
      
    working_directory: ~/repo

    steps:
      - checkout

      - run:
          name: NUnit Tests
          command: dotnet build DockerNetCore.Api.sln