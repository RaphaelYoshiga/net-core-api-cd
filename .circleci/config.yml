version: 2
jobs:
  build:
    docker:
      - image: microsoft/aspnetcore-build:2.0
      
    working_directory: ~/repo

    steps:
      - checkout

      - run:
          name: NUnit Tests
          command: dotnet build DockerNetCore.Api.sln